#include<iostream>
#include<cstdio>
#include<cstring>
#include<stack>
#include<algorithm>
using namespace std;
bool cmp(int a,int b)
{
    return a>b;
}
int main()
{
    int a[10];
    int i;
    for(i=0;i<10;i++)
    {
        cin>>a[i];
    }
    sort(a,a+10,cmp);
    for(i=0;i<10;i++)
    {
        cout<<a[i]<<' ';
    }
}

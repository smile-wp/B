#include<stdio.h>
#include<math.h>
int main()
{
   int x,y;
   scanf("%d",&x);
   if(x<0)
   {
      y=fabs(x);
       printf("%d",y);
   }
   else if(x>=0&&x<10)
   {
      y=x*x;
       printf("%d",y);
   }
   else if(x>=10)
   {
       y=log(x);
       printf("%d",y);
   }
   return 0;
}
